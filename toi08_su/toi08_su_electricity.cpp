#include <bits/stdc++.h>
using namespace std;
int main(void){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N,k;
    cin >> N >> k;
    vector<int> P(N+1);
    for (int i=1; i<=N; i++)
        cin >> P[i];
}
#include <bits/stdc++.h>
using namespace std;
int main(void){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N;
    cin >> N;
    string R;
    vector<int> dice(N,1);
    for (int i=0; i<N; i++){
        cin >> R;
    }
}
